/* pages/welcome/welcome.wxss */
/* 推荐使用 rpx 代替 px 使用 rpx 可以直接写图片的原来大小, 375px (750rpx)设计图 */
/* 1px = 2rpx iPhone6/7/8 标准机型 */
page {
  /* navigationBarBackgroundColor + page = 整个页面 */
  background-color: #b3d4db;
}
.container {
  display: flex;
  flex-direction: column;
  /* justify-content: center; */
  /* 侧轴居中 */
  align-items: center;

  /* 无法通过仅仅设置 bgc 来实现整个页面的颜色 */
  background-color: #b3d4db;
  
}

.user-avatar {
  width: 200rpx;
  height: 200rpx;
  margin-top: 160rpx;
}

.motto {
  /* color: #666666; 通用的 CSS 放到 app.wxss 中 */
  margin-top: 100rpx;
  font-size: 32rpx;
}

.journey-container {
  /* 文本水平居中 */
  /* text-align: center; */

  /* 使用 flex 进行居中 */
  display: flex;
  justify-content: center;
  align-items: center;
  border: 1px solid #405f80;
  width: 200rpx;
  height: 80rpx;
  border-radius: 5px;
  margin-top: 200rpx;
}

.journey {
  /* 文本垂直居中 */
  /* line-height: 80rpx; */
  font-size: 22rpx;
  color: #405f80;
  font-weight: bold;
}