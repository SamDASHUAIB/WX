<!--
  getuserinfo
  用户点击该按钮时，会返回获取到的用户信息，回调的detail数据与wx.getUserInfo返回的一致，open-type="getUserInfo"时有效
 -->
<button size='mini' bind:getuserinfo="onGetUserInfo" open-type='{{openType}}'
  plain='{{true}}' class="button-container">
  <slot name="img"></slot>
</button>
